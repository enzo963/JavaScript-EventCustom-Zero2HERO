
** The usually events in JS it's**
    - click
    - input
    - submit
    - keydown
    - mousemove 

All of these events and we call it ** Built -in Events **

    But what if we want create events by our selves like:

```
playerDead

userLogin

cartUpdated

themeChanged

courseFinished

```


In ** JS ** we can create events 

So now we have 2 types of events in ** JS **

    - 1 Event - in JS by default
- 2 Custom Event - you create it in JS 


### Keywords

1 - Event
2 - CustomEvent
3 - dispatchEvent()
    - this is most important Keywords in JS, It function is to trigger the event.
	
4 - addEventListener()
    - this Keywords working with CustomEvent 

5 - new CustomEvent()
    - this create the event for the first one. 

6 - detail
        - this is the most important


--- 

### dispatchEvent()

Example

    ```js 

button.dispatchEvent(event)

// Run this event now.

```



### addEventListener()

Example

    ```js

button.addEventListener("gameOver", ...)

// if 

```



### new CustomEvent()

Example

    ``` js

const event = new CustomEvent("gameOver")

// now you have an event -> gameOver

```



### detail

Example

    ```js 

detail 
  |
  v
  
//these are the data may you send it inside the meassge Like : 

// 1- score : 900   
// 2- usenmae : Enzo 
// 3- them : dark 


// The form it's like this : 

new CustomEvent("login", {

detail : {
name: "ENZO"
}

}) 


// and inside the listener : 

button.addEventListener("",{
	console.log(e.detail);
	//OR 
	console.log(event.detail);
} ) 


  

```


---


## So now we have


    | ** keywords **     | ** The  Function **             |
| ---------------- | ----------------------------- |
| Event | Witch event |
| CustomEvent | An event from your own making |
| dispatchEvent | Trigger the event |
| addEventListener | Waiting the event |
| detail | load data |
| event.detail | Read data |


    ---

# Examples 

## Example 1


    ```JS 

const btn = document.getElementById("btn");

btn.addEventListener("hello", () => {
	console.log("Hello Evnet!");
});

const myEvent = new CustomEvent("hello");

btn.dispatchEvnet(myEvent);


```

So What's happen here 

1 - We create the Event
    |
  |
    v
2 - hello
    |
  |
    v
3 - We listened to it
    |
  |
    v
4 - We trigger it
    |
  |
    v
5 - The Results

    ```js
Hello Event!
```


---


## Example 2 - Change the name

    ```js 

cosnt alarm = new CustomEvent("alarm");

document.addEventListener("alarm", ()=> {
	console.log("Danger!"); 
});

document.dispatchEvent(alarm);

// The Result is - Danger! - 

```



---

## Example 3 - Send Data

    ```js

// Send the data 
cosnt loginEvnet = neew CustomEvent("login", {
	detail: 
		username: "ENZO"

});

// New we have to listen for this event(data)

doucment.addEventLister("login",(e) => {
	console.log(e.detail.usename);
});


// Now we have to Run this Event 

document.dispatchEvent(loginEvnet);

// The Result 

ENZO

```



---

## Example 3 - Send a lot of Data

    ```js

// Send 

cosnt scoreEvent = new CustomEvent("gameFinished", {

	detail : {
		player : "Enzo",
		score  : 900,
		level  : 15 
}

});


// Listener 

document.addEventListener("gameFinished", (e)=> {
	console.log(e.detail.player);
	console.log(e.detail.score );
	console.log(e.detail.level );
});


// The Result 
/*
	Enzo
	900
	15
*/

```


---

## Example 4 - Real example(Store)

    ```js


// Send 
const cartUpdated = new CustomEvent("cartUpdated", {
	detail:{
		items:5
	}
});


// Listener

document.addEventListener("cartUpdated", (e)=> {
	console.log(
	`Item: ${ e.detail.itmes } `
	);
});

document.dispatchEvent(carUpdataed);


```



---

## Example 5 - May listeners for one event


    ```js

const event = new CustomEvent("gameOver");

document.addEventListener("gameOver", () => {
	console.log("Show Game Over screen");
});

document.addEventListener("gameOver", () => {
	console.log("Play sad music");
});

document.addEventListener("gameOver", () => {
	console.log("Save score");
});


documet.dispatchEvent(event);

```






